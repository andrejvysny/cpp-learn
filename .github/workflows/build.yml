on:
  push:
    branches:
      - master
name: Handle Release
jobs:

  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make
        run: make
      - uses: actions/upload-artifact@v3
        with:
          name: main-binary
          path: bin/main